---
export const prerender = false;

const { id } = Astro.params;
if (!id) {
  return Astro.redirect("/404");
}

// Always send /authors/:id → /authors/:id/1 (first page)
return Astro.redirect(`/authors/${id}/1`);
---
